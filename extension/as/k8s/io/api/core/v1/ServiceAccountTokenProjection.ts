// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0
//   protoc        v3.20.1

import { Writer, Reader } from "as-proto/assembly";

export class ServiceAccountTokenProjection {
  static encode(message: ServiceAccountTokenProjection, writer: Writer): void {
    writer.uint32(10);
    writer.string(message.audience);

    writer.uint32(16);
    writer.int64(message.expirationSeconds);

    writer.uint32(26);
    writer.string(message.path);
  }

  static decode(reader: Reader, length: i32): ServiceAccountTokenProjection {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new ServiceAccountTokenProjection();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.audience = reader.string();
          break;

        case 2:
          message.expirationSeconds = reader.int64();
          break;

        case 3:
          message.path = reader.string();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  audience: string;
  expirationSeconds: i64;
  path: string;

  constructor(
    audience: string = "",
    expirationSeconds: i64 = 0,
    path: string = ""
  ) {
    this.audience = audience;
    this.expirationSeconds = expirationSeconds;
    this.path = path;
  }
}
