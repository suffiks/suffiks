// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0
//   protoc        v3.20.1

import { Writer, Reader } from "as-proto/assembly";

export class ClientIPConfig {
  static encode(message: ClientIPConfig, writer: Writer): void {
    writer.uint32(8);
    writer.int32(message.timeoutSeconds);
  }

  static decode(reader: Reader, length: i32): ClientIPConfig {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new ClientIPConfig();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.timeoutSeconds = reader.int32();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  timeoutSeconds: i32;

  constructor(timeoutSeconds: i32 = 0) {
    this.timeoutSeconds = timeoutSeconds;
  }
}
