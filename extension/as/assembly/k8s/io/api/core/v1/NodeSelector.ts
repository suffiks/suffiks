// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0
//   protoc        v3.20.1

import { Writer, Reader } from "as-proto/assembly";
import { NodeSelectorTerm } from "./NodeSelectorTerm";

export class NodeSelector {
  static encode(message: NodeSelector, writer: Writer): void {
    const nodeSelectorTerms = message.nodeSelectorTerms;
    for (let i: i32 = 0; i < nodeSelectorTerms.length; ++i) {
      writer.uint32(10);
      writer.fork();
      NodeSelectorTerm.encode(nodeSelectorTerms[i], writer);
      writer.ldelim();
    }
  }

  static decode(reader: Reader, length: i32): NodeSelector {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new NodeSelector();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.nodeSelectorTerms.push(
            NodeSelectorTerm.decode(reader, reader.uint32())
          );
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  nodeSelectorTerms: Array<NodeSelectorTerm>;

  constructor(nodeSelectorTerms: Array<NodeSelectorTerm> = []) {
    this.nodeSelectorTerms = nodeSelectorTerms;
  }
}
