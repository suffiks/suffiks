// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0
//   protoc        v3.20.1

import { Writer, Reader } from "as-proto/assembly";

export class NamespaceSpec {
  static encode(message: NamespaceSpec, writer: Writer): void {
    const finalizers = message.finalizers;
    if (finalizers.length !== 0) {
      for (let i: i32 = 0; i < finalizers.length; ++i) {
        writer.uint32(10);
        writer.string(finalizers[i]);
      }
    }
  }

  static decode(reader: Reader, length: i32): NamespaceSpec {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new NamespaceSpec();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.finalizers.push(reader.string());
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  finalizers: Array<string>;

  constructor(finalizers: Array<string> = []) {
    this.finalizers = finalizers;
  }
}
