// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0
//   protoc        v3.20.1

import { Writer, Reader } from "as-proto/assembly";
import { APIGroup } from "./APIGroup";

export class APIGroupList {
  static encode(message: APIGroupList, writer: Writer): void {
    const groups = message.groups;
    for (let i: i32 = 0; i < groups.length; ++i) {
      writer.uint32(10);
      writer.fork();
      APIGroup.encode(groups[i], writer);
      writer.ldelim();
    }
  }

  static decode(reader: Reader, length: i32): APIGroupList {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new APIGroupList();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.groups.push(APIGroup.decode(reader, reader.uint32()));
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  groups: Array<APIGroup>;

  constructor(groups: Array<APIGroup> = []) {
    this.groups = groups;
  }
}
