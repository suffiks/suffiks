// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0
//   protoc        v3.20.1

import { Writer, Reader } from "as-proto/assembly";

export class RawExtension {
  static encode(message: RawExtension, writer: Writer): void {
    writer.uint32(10);
    writer.bytes(message.raw);
  }

  static decode(reader: Reader, length: i32): RawExtension {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new RawExtension();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.raw = reader.bytes();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  raw: Uint8Array;

  constructor(raw: Uint8Array = new Uint8Array(0)) {
    this.raw = raw;
  }
}
