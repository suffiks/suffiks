// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0
//   protoc        v3.20.1

import { Writer, Reader } from "as-proto/assembly";

export class EventSource {
  static encode(message: EventSource, writer: Writer): void {
    writer.uint32(10);
    writer.string(message.component);

    writer.uint32(18);
    writer.string(message.host);
  }

  static decode(reader: Reader, length: i32): EventSource {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new EventSource();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.component = reader.string();
          break;

        case 2:
          message.host = reader.string();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  component: string;
  host: string;

  constructor(component: string = "", host: string = "") {
    this.component = component;
    this.host = host;
  }
}
