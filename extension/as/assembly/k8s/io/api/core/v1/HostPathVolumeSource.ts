// Code generated by protoc-gen-as. DO NOT EDIT.
// Versions:
//   protoc-gen-as v1.3.0
//   protoc        v3.20.1

import { Writer, Reader } from "as-proto/assembly";

export class HostPathVolumeSource {
  static encode(message: HostPathVolumeSource, writer: Writer): void {
    writer.uint32(10);
    writer.string(message.path);

    writer.uint32(18);
    writer.string(message.type);
  }

  static decode(reader: Reader, length: i32): HostPathVolumeSource {
    const end: usize = length < 0 ? reader.end : reader.ptr + length;
    const message = new HostPathVolumeSource();

    while (reader.ptr < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          message.path = reader.string();
          break;

        case 2:
          message.type = reader.string();
          break;

        default:
          reader.skipType(tag & 7);
          break;
      }
    }

    return message;
  }

  path: string;
  type: string;

  constructor(path: string = "", type: string = "") {
    this.path = path;
    this.type = type;
  }
}
